// Сформувати рядок за допомогою вкладених циклів. Для перекладу рядка використовуйте \n.

// 0123456789
// 0123456789
// 0123456789
// 0123456789
// 0123456789
// 0123456789
// 0123456789
// 0123456789
// 0123456789
// 0123456789

// Вводимо кількість рядків та кількість цифр у кожному рядку
let rowCount = 10;
let digitCount = 10;

// Ініціалізуємо порожній рядок
let result = "";

// Зовнішній цикл для рядків
for (let i = 0; i < rowCount; i++) {
    // Внутрішній цикл для цифр
    for (let j = 0; j < digitCount; j++) {
        result += j;
    }
    // Додаємо перенос рядка, крім останнього рядка
    if (i < rowCount - 1) {
        result += "\n";
    }
    // result = result.slice(0, -1); - це Ви мені на пошту написали - я не зрозумів як мені ним користуватись

}

console.log(result);

// після циклу можно зробити result.slice(0, -1) замість цієї умови - ЦЕ Ви мені вказали -//
// Не дуже зрозумів...воно ж виреже останню цифрц чи пробіл...я піставлял її і так і сяк 
// - не буде ж виходить цифєрки в стовбчик